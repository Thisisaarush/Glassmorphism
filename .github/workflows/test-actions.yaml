name: Testing github actions

on:
  push:
    branches:
      - master
  pull-request:
    branches:
      - master
  workflow-dispatch:

jobs:
  greetings:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
      - uses: actions/checkout@v2
        run: echo "Hello!"
        shell: bash
  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Bye
      run: echo "Bye!"
      shell: bash
